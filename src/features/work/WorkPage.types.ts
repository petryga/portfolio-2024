export interface ImageProps {
  alt: string;
  category: string;
  index: number;
  aspectRatio: string;
}

export interface ProjectProps {
    id: string;
    name: string;
    date: number | string;
    src: string;
    link: string;
    skills: string;
}